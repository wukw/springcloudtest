server:
  port: 8084

eureka:
  instance:
    hostname: eureka2
    prefer-ip-address: true
#    statusPageUrl: https://${eureka.hostname}/info
#    healthCheckUrl: https://${eureka.hostname}/health
#    homePageUrl: https://${eureka.hostname}/
  client:
#    registerWithEureka: false
#    fetchRegistry: false
    serviceUrl:
      defaultZone: http://eureka1:8083/eureka/

